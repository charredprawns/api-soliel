{
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "jest": "^25.2.0",
    "mongodb": "^3.5.3",
    "mongoose": "^5.8.11"
  },
  "scripts": {
    "dev": "NODE_PATH=src nodemon -r dotenv/config src/server.js",
    "start": "NODE_ENV=production NODE_PATH=src node src/server.js",
    "debug": "NODE_PATH=src nodemon -r dotenv/config --inspect src/server.js",
    "test": " jest"
  },
  "devDependencies": {
    "@testing-library/react": "^10.0.1",
    "nodemon": "^2.0.2"
  }
}
